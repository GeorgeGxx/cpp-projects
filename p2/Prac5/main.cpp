#include <stdio.h>
#include <conio.h>
#include <windows.h>
#include <string.h>

char origen;
float precio, sumagral, sumanac, sumaimport, promnac, promimport;
int contador, contanac, contaimp;
int main()
{
	system("cls");
	for(conta=1;conta<=10;conta++)
	{
		fflush(stdin);
		printf("captura precio del auto ");
		scanf("%f",&precio);
		fflush(stdin);
		printf("es nacional? s/n: ");
		scanf("%c",&origen);
		fflush(stdin);
		if(origen=='s')
		
	}
}
